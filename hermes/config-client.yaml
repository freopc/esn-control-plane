model: gemini-flash
agent:
  reasoning_effort: minimal
toolsets:
  enabled:
    - kanban
    - memory
  disabled:
    - code_execution
    - browser
    - image_generation
    - web_search
max_tokens_per_turn: 2500
usage_file_mandatory: true
